apiVersion: v1
kind: Service
metadata:
  name: {{ include "data-claims-reporting.fullname" . }}
  labels:
    {{- include "data-claims-reporting.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "data-claims-reporting.selectorLabels" . | nindent 4 }}
