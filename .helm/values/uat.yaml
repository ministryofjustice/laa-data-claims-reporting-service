# Default values for uat helm chart.

replicaCount: 1

image:
  #  Values will be overwritten by Cloud Platform config
  repository: null
  tag: null

serviceAccount:
  name: "data-claims-reporting"

service:
  type: ClusterIP
  port: 8080

resources:
  limits:
    cpu: 1000m
    memory: 1000Mi
  requests:
    cpu: 10m
    memory: 100Mi
